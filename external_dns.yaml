---
# # Roll-your-own DNS
# Update the values below with the keys/IP listed with the post-provision playbook
external_nsupdate_keys:
  public-openshift.cluster.lc:
    #key_secret: ''
    #key_algorithm: 'hmac-sha256'
    #server: ''
    instack: true
  private-openshift.cluster.lc:
    #key_secret: ''
    #key_algorithm: 'hmac-sha256'
    #server: ''
    instack: true
    public_access: true
